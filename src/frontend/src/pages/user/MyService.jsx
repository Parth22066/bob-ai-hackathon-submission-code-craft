import { BellRing, CheckCircle2, CloudRain, MapPin, ShieldCheck, TriangleAlert, Zap } from "lucide-react";
import { Link } from "react-router-dom";
import { userProfile } from "../../data/userData";
import UserPageHeader from "./UserPageHeader";
function MyService() { const details = [["Connection ID", userProfile.connectionId], ["Customer name", userProfile.name], ["Service location", userProfile.serviceLocation], ["Area", userProfile.area], ["Service type", userProfile.serviceType], ["Last updated", userProfile.lastUpdated]]; return <><UserPageHeader title="My Service" description="View your electricity service information and local status." /><div className="grid gap-6 lg:grid-cols-[1.1fr_.9fr]"><section className="rounded-2xl border border-emerald-200 bg-white p-6 shadow-sm"><div className="flex gap-4"><span className="flex h-12 w-12 shrink-0 items-center justify-center rounded-xl bg-emerald-50 text-emerald-600"><Zap size={24} fill="currentColor" /></span><div><p className="text-sm font-semibold text-slate-500">Electricity Service</p><h2 className="mt-1 flex items-center gap-2 text-2xl font-bold"><CheckCircle2 className="text-emerald-500" />Operational</h2><p className="mt-2 text-sm text-slate-500">Your electricity service is currently operating normally.</p></div></div><dl className="mt-7 divide-y divide-slate-100 border-t border-slate-100">{details.map(([label, value]) => <div key={label} className="flex flex-col gap-1 py-3 text-sm sm:flex-row sm:justify-between"><dt className="text-slate-500">{label}</dt><dd className="font-semibold text-slate-700">{value}</dd></div>)}</dl></section><aside className="space-y-4"><Status icon={ShieldCheck} label="Area Risk" value={userProfile.areaRisk} description="Your area has a low risk of service disruption." tone="blue" /><Status icon={CloudRain} label="Weather Risk" value={userProfile.weatherRisk} description="Weather conditions are being monitored." tone="amber" /><div className="rounded-2xl border border-slate-200 bg-white p-5 shadow-sm"><div className="flex items-center gap-2 text-sm text-slate-500"><MapPin size={17} />Service area</div><p className="mt-2 font-bold text-slate-900">{userProfile.serviceLocation}</p></div></aside></div><div className="mt-6 flex flex-col gap-3 sm:flex-row"><Link to="/user/outages" className="inline-flex items-center justify-center gap-2 rounded-lg bg-slate-900 px-4 py-2.5 text-sm font-semibold text-white"><TriangleAlert size={17} />View Outages</Link><Link to="/user/alerts" className="inline-flex items-center justify-center gap-2 rounded-lg border border-slate-300 bg-white px-4 py-2.5 text-sm font-semibold text-slate-700"><BellRing size={17} />View Alerts</Link></div></>; }
function Status({ icon: Icon, label, value, description, tone }) { return <div className="rounded-2xl border border-slate-200 bg-white p-5 shadow-sm"><Icon size={20} className={tone === "amber" ? "text-amber-600" : "text-blue-600"} /><p className="mt-3 text-sm text-slate-500">{label}</p><p className="mt-1 text-xl font-bold">{value}</p><p className="mt-2 text-sm text-slate-500">{description}</p></div>; }
export default MyService;
