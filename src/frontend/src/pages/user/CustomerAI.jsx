import { BrainCircuit, Send } from "lucide-react";
import { useState } from "react";
import { userProfile } from "../../data/userData";
import UserPageHeader from "./UserPageHeader";
const answers = { "Is there an outage in my area?": `There is currently no active outage reported in ${userProfile.serviceLocation}. Your electricity service is operating normally.`, "Why is my service at risk?": "Your area has a low overall service risk. Moderate weather conditions are being monitored, while your service continues to operate normally.", "When will my power be restored?": "There is no active outage affecting your service. If an interruption occurs, restoration information will be shown in the Outages section.", "How can I report a problem?": "You can report a power outage, voltage issue, or electrical problem using the Report an Issue section." };
function CustomerAI() { const [messages, setMessages] = useState([]); const [input, setInput] = useState(""); const ask = (question) => { const text = question.trim(); if (!text) return; const response = answers[text] || "I can help with your service status, local outages, weather alerts, and reporting a problem. Try one of the suggested questions below."; setMessages([...messages, { from: "user", text }, { from: "ai", text: response }]); setInput(""); }; return <><UserPageHeader title="GridGuard AI Assistant" description="Get help with your electricity service." /><section className="overflow-hidden rounded-2xl border border-slate-200 bg-white shadow-sm"><div className="flex items-center gap-3 border-b border-slate-100 p-5"><span className="flex h-10 w-10 items-center justify-center rounded-xl bg-blue-50 text-blue-600"><BrainCircuit size={21} /></span><div><p className="font-bold">GridGuard AI</p><p className="text-sm text-slate-500">Customer service assistant</p></div></div><div className="min-h-72 space-y-4 bg-slate-50 p-5">{messages.length ? messages.map((message, index) => <div key={index} className={`max-w-xl rounded-xl px-4 py-3 text-sm leading-6 ${message.from === "user" ? "ml-auto bg-blue-600 text-white" : "bg-white text-slate-700 shadow-sm"}`}>{message.text}</div>) : <div className="py-12 text-center"><BrainCircuit className="mx-auto text-blue-300" size={32} /><p className="mt-3 font-semibold text-slate-700">How can I help?</p><p className="mt-1 text-sm text-slate-500">Ask about your electricity service, outages, or alerts.</p></div>}</div><div className="border-t border-slate-100 p-4"><div className="mb-3 flex flex-wrap gap-2">{Object.keys(answers).map((question) => <button key={question} onClick={() => ask(question)} className="rounded-full border border-slate-200 bg-white px-3 py-1.5 text-left text-xs font-medium text-slate-600 hover:border-blue-200 hover:text-blue-700">{question}</button>)}</div><form onSubmit={(event) => { event.preventDefault(); ask(input); }} className="flex gap-2"><input value={input} onChange={(event) => setInput(event.target.value)} placeholder="Ask GridGuard AI..." className="input" /><button aria-label="Send message" className="rounded-lg bg-blue-600 p-2.5 text-white"><Send size={19} /></button></form></div></section></>; }

export default CustomerAI;
